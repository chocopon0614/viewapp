version: 2.1

jobs:
  test:
      machine: 
        image: default
      steps:
        - checkout
        - restore_cache:
            key: circleci-java-maven-{{ checksum "pom.xml" }}
        - run:
            command: mvn clean install
        - save_cache:
            paths:
              - ~/.m2
            key: circleci-java-maven-{{ checksum "pom.xml" }}
        - persist_to_workspace:
            root: '.'
            paths:
              - target
        - run:
            name: Save test results
            command: |
              mkdir -p ~/test-results/junit/
              find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} ~/test-results/junit/ \;
            when: always
        - store_test_results:
            path: ~/test-results
        - store_artifacts:
            path: ~/test-results/junit

  deploy:
      machine: 
        image: default
 #     working_directory: ~/
      steps:
#        - attach_workspace:
#            at: '.'
        - run:
            name: "preparing"
            command: |
               ls -l /home/circleci/
#               tar -zxvf /home/circleci/ibmcloud/IBM_Cloud_CLI_2.1.1_amd64.tar.gz
#               sudo curl --insecure https://raw.githubusercontent.com/IBM-Cloud/ibm-cloud-developer-tools/master/linux-installer/idt-installer | bash
#              ibmcloud dev help
#              sudo apt-get update
              

workflows:
  test-deploy:
    jobs:
      - deploy
#      - test
#      - deploy:
#            requires:
#                - test
#           filters:
#               branches:
#                 only: master
